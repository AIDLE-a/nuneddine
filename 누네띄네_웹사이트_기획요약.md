# 누네띄네 (NuneDDine) 웹사이트 기획 요약

---

## 서비스 개요

**누네띄네**는 주식 AI 분석 플랫폼이다.  
국내외 주식을 검색하면 뉴스 감성 분석, 7일 가격 예측, XAI 요인 분석, AI 리포트를 자동으로 제공한다.  
로그인(Google)하면 관심 종목 저장(매수가·수량·손익 관리), 분석 기록, 커뮤니티 기능이 활성화된다.  
ESP32-C3 + ST7789 다마고치 하드웨어와 BLE로 연동해 관심 종목 현황을 캐릭터 표정으로 실시간 확인할 수 있다.

---

## 팀 구성

| 팀원 | 역할 |
|------|------|
| 희선 | A · 시스템/백엔드 리더 — FastAPI 전체 구조, 에이전트 orchestration, Prophet 예측 |
| 유빈 | B · 데이터 수집 — yfinance 주가, Naver·Google RSS·NewsAPI 뉴스 수집·전처리 |
| 연우 | C · AI/ML — FinBERT 감성 분석, XAI(Leave-one-out 단어 기여도), 트렌드·변동성 |
| 채민 | D · 프론트엔드 + Arduino — React/Vite UI, CSS 디자인 시스템, ESP32 ST7789 다마고치, BLE |

---

## 화면 구조

### 레이아웃
- **사이드바 (왼쪽 고정, 248px)** + **메인 콘텐츠 영역**
- 라이트 / 다크 모드 토글 지원 (CSS 커스텀 프로퍼티 기반, 테마 토큰 `--accent: #0D9488`)

---

## 페이지별 내용

### 1. 분석 페이지 (메인, `/`)

**상단: 종목 검색 바**
- 종목명 또는 코드 입력 → 자동완성 드롭다운
- 드롭다운 안에 **인기 종목** (최근 1시간 내 분석 횟수 순, 최대 6개) 실시간 표시
- 각 인기 종목에 현재가 · 등락률 표시

**우측: 관심 종목 패널 (WatchlistPanel)**
- 로그인 시 관심 종목 목록 + 분석 기록 표시
- 관심 등록/해제 버튼 (★)

**메인 카드 영역 (6개 카드)**

| 카드 | 내용 |
|------|------|
| **요약 카드** (SummaryCards) | 현재가, 등락률, 7일 예측가, 예측 범위 |
| **주가 차트** (StockChartCard) | 실제 주가 + 7일 예측 점선 라인 차트 |
| **감성 분석** (SentimentCard) | 긍정/부정/중립 비율, XAI 단어 기여도 바 |
| **신뢰도** (ReliabilityCard) | 종합 신뢰도 점수(0~100), 뉴스 수집 건수 |
| **최근 뉴스** (RecentNewsCard) | 관련 뉴스 목록 (출처, 시간, 긍/부정 태그) |
| **AI 리포트** (AiReportCard) | Critic 에이전트 분석 요약 + 경고 배너 |

**로딩 순서 (분석 중 메시지)**
1. 뉴스 및 주가 데이터 수집 중...
2. FinBERT 감성 분석 중...
3. Prophet 7일 예측 계산 중... (약 30~60초 소요)
4. Critic 에이전트 신뢰도 검토 중...

---

### 2. 커뮤니티 페이지 (`/community`)
- 게시글 목록 (종목별 토론)
- 로그인 사용자 글쓰기 / 좋아요 / 댓글 가능
- 게시글 상세 페이지 (`/community/:postId`)

### 3. 관심 종목 페이지 (`/favorites`)
- 저장한 관심 종목 리스트 (인기 종목과 동일한 레이아웃)
- **매수가 · 매수 통화 · 매수 수량 입력** → Firestore 실시간 저장
- 총 손익 금액 · 손익률 계산 표시 `(현재가 - 매수가) × 수량`
- 탭 필터: 전체 / 수익 / 손실
- 종목 클릭 시 분석 페이지로 이동, 관심 해제 가능

### 4. 분석 기록 페이지 (`/history`)
- 과거 분석한 종목 목록
- 삭제 가능, 클릭 시 재분석

### 5. 인기 종목 랭킹 페이지 (`/ranking`)
- 분석 횟수 기준 실시간 랭킹 (최대 6개)
- 현재가 · 등락률 표시, 분석 바로가기 버튼

### 6. 마이페이지 (`/mypage`)
- 프로필 이미지 (사진 크롭 기능 포함)
- 다크 모드 토글
- 관심 종목 / 기록 현황
- 로그아웃

---

## 사이드바 메뉴

| 메뉴 | 경로 |
|------|------|
| 분석 | `/` |
| 커뮤니티 | `/community` |
| 관심 종목 | `/favorites` |
| 분석 기록 | `/history` |
| 랭킹 | `/ranking` |
| 마이페이지 | `/mypage` |

- 로그인/로그아웃 버튼 (Google 계정)
- 다크 모드 토글
- 알림 벨 (NotificationBell)

---

## 주요 기능

### 주식 검색
- 국내(KRX: `.KS`, `.KQ`) + 해외(NASDAQ, NYSE 등) 지원
- 종목명 / 코드 / 별칭으로 검색 가능 (예: "삼전", "samsung" → 삼성전자)

### 분석 엔진 (AI 파이프라인)
1. **유빈**: yfinance 주가 + Naver·Google RSS·NewsAPI 뉴스 수집·전처리
2. **연우**: FinBERT 가중치 감성 분석 + 트렌드·변동성 계산 + XAI 단어 기여도
3. **희선**: Prophet 7일 가격 예측 (신뢰구간 포함) + Critic 에이전트 경고·신뢰도 산출

### 관심 종목 손익 관리
- 매수가 / 통화(₩·$) / 수량 입력 → Firestore 저장
- 실시간 손익 = `(현재가 - 매수가) × 수량`

### 가격 표시 단위
- 국내 주식: 원화 (₩)  
- 해외 주식: 달러 ($)  
- `currencyUtils.js`에서 ticker 접미사(`.KS`, `.KQ`)로 자동 감지

### 로그인
- Firebase Google 로그인
- 관심 종목 · 기록은 Firestore에 저장 (`users/{uid}`)

### 다마고치 하드웨어 연동
- ESP32-C3 + ST7789 240×240 TFT 디스플레이
- 로그인 시 백엔드에 UID 등록 (`POST /api/active-user`)
- `ble_sender.py` (5분 주기) → `/api/watchlist` + `/api/tamagotchi` → BLE 전송
- BLE 포맷: `"종목명,등락률,현재가,통화,매수가,수량|..."` (최대 6개)
- 감정 상태 6단계: HAPPY(+5%↑) / SEMI-HAPPY(+2~5%) / NORMAL(±2%) / TIRED(-2~-5%) / CRYING(-5~-10%) / SICK(-10%↓)
- 등락 변화 시 버저 알림

---

## 분석 예시 종목 (기본 제공 목 데이터)

| 종목 | 코드 | 특이사항 |
|------|------|----------|
| 삼성전자 | 005930.KS | HBM4 이슈 |
| SK하이닉스 | 000660.KS | AI 메모리 수혜주 |
| 테슬라 | TSLA | 유럽·중국 수요 둔화 |
| Apple | AAPL | 서비스 매출 신기록 |

---

## 브랜드 아이덴티티

- **이름**: 누네띄네 / NuneDDine
- **의미**: "눈에 띄는" → DD = 두 개의 큰 눈(👀)
- **컬러**: 라이트 `--accent: #0D9488` (teal), 다크 `--accent: #2DD4BF` (bright teal), 배경 `#F2FAF8` / `#071410`
- **다마고치 캐릭터**: 민트색 몸통, D자 눈, 주가 상태에 따른 6가지 표정 변화

---

## 기술 스택

| 구분 | 기술 |
|------|------|
| 프론트엔드 | React 18 + Vite, React Router, Recharts, Firebase SDK |
| 백엔드 | FastAPI (Python), uvicorn |
| 인증/DB | Firebase Auth + Firestore |
| AI/ML | FinBERT, Prophet, XAI (Leave-one-out), Critic Agent |
| 데이터 | yfinance, Naver 뉴스 API, Google RSS, NewsAPI |
| 하드웨어 | ESP32-C3 + ST7789 240×240 TFT + Passive Buzzer + BLE (bleak) |
